public void moveZeroes(int[] nums) {
        
        int left = 0;//tracks zeroes 
        for(int right = 0; right < nums.length; right++)
        {
            if(nums[right]!=0)//found a non zero swap with left
            {
                int temp = nums[left];
                nums[left] = nums[right];
                nums[right] = temp;
                left++;
            }
        }
        return;
    }
